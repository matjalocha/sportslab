services:
  api:
    volumes:
      - ./apps/api/src:/app/src
      - ./packages:/app/packages
    environment:
      SPORTSLAB_ENV: dev
      SPORTSLAB_LOG_LEVEL: DEBUG
    command: uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
